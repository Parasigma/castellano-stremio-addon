# Omitir dependencias OPCIONALES (módulos nativos como utp-native, bufferutil,
# utf-8-validate). Evita errores de compilación en PCs sin herramientas de build;
# el addon funciona perfectamente sin ellos (WebTorrent usa TCP en vez de uTP).
omit=optional
fund=false
audit=false
